# Basic Revue App

Minimal TUI application with revue.

## Run

```bash
cargo run
```

Press `q` to quit.

## What's included

- Simple vertical layout with `vstack()`
- Text widgets with preset styles
- Basic event handling
